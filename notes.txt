Combobox:
How to limit number of results in Command dropdown?
How to get combobox to automatically highlight top item?

GroupManager:
Confirm that data filtering solution actually works
STEP 3 CHECK POD instead of Component for SourcePodID for efficiency?
Restyle into a simple sensible level at first

ComponentCode:
Add onclicks
Decide on UI design

TanStack Query settings:
Edit defaults, (no stale data, etc)

Errors:
Runtime error boundary?
Implement error visual component?
Use library? Toastify, or DIY it

Badges:

DarkMode:
Edit to toggle instead of select, default: system

Router:
Implement Tanstack Router?
What pages to have in future?
- Compatibility
- About
